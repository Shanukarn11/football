DEBUG=True
SECRET_KEY=development-only-change-me
APP_ALLOWED_URL=localhost
CSRF_TRUSTED_ORIGINS=

# Enable these only behind a working HTTPS reverse proxy.
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

DB_NAME=indiakhelofootball
DB_USER=ikf
DB_PASSWORD=change-me
DB_HOST=host.docker.internal
DB_PORT=3306

LAT_DB_NAME=latmfks
LAT_DB_USER=ikf
LAT_DB_PASSWORD=change-me
LAT_DB_HOST=host.docker.internal
LAT_DB_PORT=3306

EMAIL_HOST=localhost
EMAIL_PORT=587
EMAIL_USE_TLS=False
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Override this on production if a different registry/image is used.
IKF_IMAGE=ghcr.io/shanukarn11/football:latest
